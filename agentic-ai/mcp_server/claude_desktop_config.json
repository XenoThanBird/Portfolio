{
  "mcpServers": {
    "mcp-template": {
      "command": "python",
      "args": ["-m", "mcp_server.server"],
      "env": {
        "MCP_SERVER_NAME": "mcp-template",
        "TIMEOUT_SECONDS": "30",
        "MAX_RETRIES": "3",
        "LOG_LEVEL": "INFO"
      }
    }
  }
}
