# The Sentinel Script — Environment Variables
# Copy to .env and fill in values as needed.

# Optional: Override config.yaml watch paths (comma-separated)
# SENTINEL_WATCH_PATHS=/etc,/var/www,/opt/app

# Optional: Webhook URL for alert delivery
# SENTINEL_WEBHOOK_URL=https://hooks.example.com/sentinel

# Optional: Override log level
# SENTINEL_LOG_LEVEL=INFO
